-- This file stores a few settings that are intentionally limited
-- compared to rc.lua, which makes them simpler to use and understand.
-- Note: a /something in a discription indicates a directory
-- within awesome's config directory.

-- Whether to use tiling or floating by default
-- Possible values:
-- 'tile', 'float'
WM_MODE = 'tile'

-- Theme. See /themes for available options.
WM_THEME = 'nord'